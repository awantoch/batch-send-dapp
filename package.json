{
  "name": "batch-send-dapp",
  "version": "1.0.0",
  "description": "Batch Send dApp",
  "main": "index.html",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "parcel -d build/frontend index.html",
    "build": "parcel build -d build/frontend --public-url ./ index.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build/frontend",
    "prepush": "npm run deploy"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "web3": "^1.0.0-beta.55"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3"
  }
}
